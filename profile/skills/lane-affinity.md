---
title: Lane Affinity
parent: Skills
last_updated: 2026-05-31
---

# Lane Affinity

> Per-lane evidence pack derived from `profile/projects/*.md` and `profile/skills/*.md`.
> Lane list source: `profile/career-goals.md` (8 active lanes, junior phase, no priority weighting).
> Audience: grading subagents (`grade-companies`, `grade-jobs`) embedding this file verbatim.

---

## big-tech — Big Tech (Generalist SWE)

**Pinnacle evidence**: No pinnacle evidence in current portfolio. The portfolio is research-heavy and systems-leaning; there is no flagship project demonstrating generalist platform-scale engineering at a FAANG-shaped distributed-systems surface (massive-scale storage, search infra, ranking systems, internal-platform tooling). The closest signal is breadth itself — shipping eight production-shaped systems across distinct domains demonstrates the generalist's range — but generalist SWE pinnacle is normally pattern-matched to one concretely-recognisable big-tech-shaped artefact, which is absent.

**Supporting evidence**: **Cernio** — multi-layer Rust system end-to-end (lib + binary + CLI pipeline + TUI + embedded web server + AI orchestration) over SQLite with strict downward dependency direction; demonstrates the architect-and-ship-a-system competence big-tech ladders test for. **Image Browser** — production-grade self-observability (perf reports, typed IPC errors, profiling-first culture) plus end-to-end Tauri 2 + React 19 + ONNX Runtime delivery. **Aurix** — clean two-runtime separation with cross-runtime contract discipline, 19 IPC handlers, 12 storage submodules with idempotency-by-design. **Performance Profiler** — 148 commits in three days shipping v0.12 with multi-agent parallel development under locked contracts. **Open Source Contributions** — merged-track contribution to a major Rust ML framework (burn A-FINE, +1864 lines, approved). All five demonstrate ship-it discipline across diverse stacks.

**Strongest cross-cutting skills**: Rust [Proficient] (from languages.md), TypeScript [Comfortable] (languages.md), Python [Comfortable] (languages.md), SQLite (WAL mode) [Proficient] (engines-runtimes.md), Tauri 2 [Proficient] (frameworks.md), Documentation-first iteration [Proficient] (concepts-domains.md), Test-driven validation [Comfortable] (concepts-domains.md), Multi-agent / worktree-isolated subagents [Comfortable] (tools-platforms.md), Git / GitHub [Proficient] (tools-platforms.md).

**Gaps for this lane**: No distributed-systems work at scale (no Kafka, no Spanner, no Iceberg, no Trino, no production-shaped microservices); no Cloud / Kubernetes / Docker / Terraform / CI-CD evidence (the #1 gap per Cernio's portfolio-gaps.md — flagged across 5+ consecutive grading batches); no large-team collaboration signals (every project is solo-author, master-only); no public-cloud (AWS / GCP / Azure) deployment evidence; no observability-stack experience (Prometheus / Grafana / Datadog / OpenTelemetry beyond bespoke per-project telemetry); no production-traffic SLO/SLI thinking; no on-call experience; no production incident postmortems. Big-tech generalist roles will hit "junior + no obvious big-tech-shaped flagship" as the dominant filter — pinnacle expectation is breadth-of-shipped-systems plus one concretely-recognisable distributed-systems artefact, and only the first half is present.

---

## ai-ml — AI / ML (Research + Engineering)

**Pinnacle evidence**: **NeuroDrive** — handwritten PPO with clipped surrogate objective and GAE, asymmetric actor-critic, plus a from-scratch biologically-plausible learning substrate (three-factor plasticity with eligibility traces, biological homeostasis on two cadences, continual-backprop structural plasticity with mature-neuron replacement + plateau-triggered neurogenesis + prune/sprout) running side-by-side against the PPO baseline; zero external ML framework dependency; 80 `.rs` files, 133 tests including 21 brain-pipeline tests; 21× frame-time improvement on M2 hardware via flat row-major weight buffers + dual GEMM backend with Apple Accelerate AMX. This is the strongest single-domain evidence in the entire portfolio for any concept.

**Supporting evidence**: **Image Browser** — three ONNX-Runtime image encoders (CLIP ViT-B/32, DINOv2-Base, SigLIP-2 Base 256) + two text encoders + Reciprocal Rank Fusion (Cormack 2009, k=60) for multi-encoder retrieval; cross-encoder preprocessing discipline (CLIP-native mean/std, bicubic-shortest-edge, L2-normalize); pipeline-versioned embedding migration. **AsteroidsAI** — four optimisation paradigms implemented from scratch (GA, diagonal CMA-ES, NEAT, Soft Actor-Critic with GNN backbone via `torch_geometric` `GATv2Conv`); graph-native replay buffer; multi-objective Pareto-front selection. **Open Source Contributions / burn A-FINE PR #4894** — published-paper computer-vision metric inlined into burn's `crates/burn-train/src/metric/vision/afine/` with CLIP ViT backbone + five A-FINE heads + PyTorch-weight loader + regression tests; +1864 lines across 10 files, maintainer-approved. **Open Source Contributions / tinygrad LSTM PR #16119** — minimum ONNX LSTM operator (14 tokenised lines), Silero VAD parity-tested, passing 2 of 4 upstream ONNX conformance LSTM tests. **Tessarix** — empirical local-LLM model selection with documented per-model failure modes; prompt discipline as load-bearing. **Consilium** — multi-agent LLM orchestration with structured-state-bottleneck design replacing free-form prose summarisation.

**Strongest cross-cutting skills**: Reinforcement learning [Proficient] (concepts-domains.md), Computer vision / vision-language models [Comfortable] (concepts-domains.md), LLM systems / prompt engineering [Comfortable] (concepts-domains.md), Multi-encoder rank-fusion retrieval [Comfortable] (concepts-domains.md), ONNX Runtime [Comfortable] (engines-runtimes.md), Rust [Proficient] (languages.md), Python [Comfortable] (languages.md), torch_geometric [Familiar] (libraries.md), Reproducibility / determinism engineering [Comfortable] (concepts-domains.md).

**Gaps for this lane**: No production-scale ML — NeuroDrive is M2-MacBook-Air scale (the A-vs-S boundary at Apple AiDP / Jane Street ML / DRW ML is 10K-GPU / petabyte scale); no CUDA / GPU-systems / PTX / SASS / CUTLASS / Triton / NCCL work (distinct from "production-scale ML" — this is GPU-kernel engineering, currently absent); no published research papers; no formal ML publications track-record; no large-scale distributed training (DDP / FSDP / Megatron); no inference-serving infrastructure (vLLM / TGI / TensorRT) at scale; no fine-tuning of large foundation models (LoRA / QLoRA / DPO at scale); no public benchmark contributions; no MLOps / experiment-tracking infrastructure (W&B / MLflow at production scale). The substantive depth (handwritten RL stack from primitives) is exceptional for junior phase, but pinnacle ML roles at frontier labs typically expect either published research or 10K-GPU production experience, neither of which is present.

---

## hft — High-Frequency Trading / Low-Latency Systems

**Pinnacle evidence**: **Nyquestro** — from-scratch multi-instrument deterministic limit-order matching engine in safe Rust (zero `unsafe`), strict price-time priority with `BtreeMap<Px, PriceLevel>` ladders + `VecDeque<Order>` FIFO per level, four-phase deterministic `submit_limit` (snapshot pre-state → aggressive matching → self-match handling → rest aggressor), snapshot-and-compare top-of-book quote semantics; multi-instrument routing via `Symbol(u64)` 8-byte ASCII pack with O(1) hash/order; backpressure as structural property (`SNAPSHOT_LEVEL_CAP=50` + `PER_FRAME_BUDGET=500` two-pronged defence); live Coinbase Advanced Trade `level2` WebSocket integration with idempotent L2-to-virtual-order translation via `HashMap<(Symbol, Side, Px), OrderID>`; per-op HDR-histogram latency percentiles across `Op::Submit/Match/Cancel` (p50/p95/p99/p999/p9999/max); microstructure inspection surface (`microprice()`, `ofi(n)`, `spread_cents()`, `depth(n)`, `top_n_bids/asks(n)`); determinism test pinning byte-identical engine output across runs; six-pane Ratatui dashboard with infographics + custom 1/8-cell precision renderer. The single most directly hft-relevant artefact in the portfolio.

**Supporting evidence**: **Tectra** — C++20 trading-infrastructure scaffold with virtual-clock-first ordering (Clock interface + RealClock via `steady_clock` for monotonicity vs NTP backwards-jumps + VirtualClock with `advance`/`set_time` for replay seeking; `const Clock&` for services so they cannot mutate virtual time); `-Wall -Wextra -Wpedantic -Werror` from day one. Status: dormant after 6 days of work; substrate only — no matching engine, no feed handler. **Aurix** — Q64.96 fixed-point math (clean-room V3 port on `BigUint` with magic-constant precomputation); cross-DEX arbitrage detection across 5 chains × 3 protocols; per-swap LP backtest engine with LVR discrete approximation; statistical awareness around selection-bias-inflated Sharpe (Bailey/de Prado Deflated Sharpe acknowledged). **Cernio** — Tokio async runtime + SQLite WAL state engine; demonstrates production-shaped async-Rust ergonomics relevant to feed-handler-shaped systems.

**Strongest cross-cutting skills**: Rust [Proficient] (languages.md), Market microstructure / limit-order-book engineering [Comfortable] (concepts-domains.md), Lock-free concurrency / safe-Rust systems [Comfortable] (concepts-domains.md), HFT-style observability / tail latency [Comfortable] (concepts-domains.md), Reproducibility / determinism engineering [Comfortable] (concepts-domains.md), Backpressure as structural property [Comfortable] (concepts-domains.md), Tokio [Proficient] (frameworks.md), hdrhistogram [Familiar] (libraries.md), Coinbase Advanced Trade WebSocket [Familiar] (engines-runtimes.md), C++ [Familiar] (languages.md).

**Gaps for this lane**: No production kdb+/q experience (Jane Street / HRT / Squarepoint expectation); no FPGA / hardware-acceleration work; no published sub-microsecond latency benchmarks (Nyquestro has HDR histograms in `Op::Submit/Match/Cancel` but no claimed sub-µs floor and no criterion benchmark harness); no actual lock-free order book yet — Nyquestro shipped the foundation layer (typed primitives + BTreeMap matching engine) but the lock-free matching engine is roadmap, not built; no STP (self-trade prevention), no journal, no risk guard, no strategy/market-making agent; no kernel bypass (io_uring / AF_XDP / DPDK); no FIX 4.4/5.0 / SBE / OUCH / ITCH protocol experience; C++ is Familiar (capping fit at firms with strict C++-mandate like Citadel / Jane Street / Tower / QRT — the C++ gap is the #1 lane blocker on 7+ roles in 2026-04-29 batch); no OCaml (Jane Street primary); order-book engine is single-venue, not multi-venue cross-routing. Nyquestro is the pinnacle for the lane on its strict price-time + safe-Rust + live-feed shape; HFT-firm graders will read it as a strong junior-phase signal but cap on the missing C++ + missing actual-lock-free + missing kernel-bypass dimensions.

---

## crypto-mm — Market-Making / DeFi Engineering

**Pinnacle evidence**: **Aurix** — local-first, zero-cost, read-only DeFi analytics desktop application with a Uniswap V3 LP backtester (Vector A) as the centre of gravity: clean-room port of Uniswap V3's mathematics stack (TickMath / FullMath / LiquidityAmounts / SqrtPriceMath) onto `num-bigint::BigUint` with 20 magic constants precomputed; per-swap LP simulation with LVR discrete approximation per Milionis-Moallemi-Roughgarden; rebalance rules (Static / Schedule / OutOfRange) with explicit recentring + gas-cost accounting; adaptive-tercile vol-regime classifier + multi-variant capital-allocation verdict (Best / Naive / Median) defeating single-variant selection-bias readings; three-tier free-data fallback architecture (subgraph → user-Alchemy → public-RPC); cross-chain DeFi system design across 5 chains × 3 protocols (Uniswap V3 + Sushi V3 + Pancake V3); 139 backend tests; explicitly framed by LifeOS as "the crypto-domain hiring project". The crypto-mm pinnacle in the portfolio.

**Supporting evidence**: **Nyquestro** — live Coinbase Advanced Trade `level2` WebSocket integration for real BTC-USD / ETH-USD / SOL-USD depth feeding a from-scratch deterministic matching engine; demonstrates the live-exchange-feed plus matching-engine pair that crypto-mm firms (B2C2, GSR, Wintermute) build their stacks on. **Cernio** — SQLite-WAL state engine + Tokio async runtime + cross-system contract discipline (demonstrates the production-shaped Rust async ergonomics relevant to crypto exchange-integration work).

**Strongest cross-cutting skills**: Quantitative / DeFi finance [Comfortable] (concepts-domains.md), Cross-chain DeFi / on-chain decoding [Familiar] (concepts-domains.md), Market microstructure / limit-order-book engineering [Comfortable] (concepts-domains.md), Rust [Proficient] (languages.md), num-bigint [Comfortable] (libraries.md), Ethereum JSON-RPC + The Graph hosted subgraph [Comfortable] (engines-runtimes.md), Tokio [Proficient] (frameworks.md), HFT-style observability / tail latency [Comfortable] (concepts-domains.md), Reproducibility / determinism engineering [Comfortable] (concepts-domains.md).

**Gaps for this lane**: No actual on-chain transaction submission — Aurix is read-only by design (cannot be extended to execution without principle violation); no MEV / mempool / Flashbots experience; no production CEX market-making engine experience; no actual exchange-connectivity production code (Coinbase WebSocket integration is single-venue and read-only); no FIX adapter, no SBE, no OUCH; no production-shaped order management system (OMS) or smart-order routing (SOR); no on-chain settlement / institutional digital-asset rails experience (Fireblocks, Anchorage); no formal stablecoin / market-making strategy implementation (just LP backtest, which is closer to LP analytics than market-making); no live-trading PnL track record; no quant research validation infrastructure (Aurix Deflated Sharpe correction is acknowledged-but-not-implemented). Aurix is the strongest junior crypto-mm signal in the portfolio but caps below pinnacle at firms that read it as "analytics tooling, not market-making". Live-data path also currently non-functional pending Alchemy key restoration.

---

## bank-strats — Bank S&T Engineering (e-Trading, Strategist Tooling)

**Pinnacle evidence**: No pinnacle evidence in current portfolio. Bank-strats roles (GS Strats, JPM QR, Citi e-trading, Barclays Strats) expect production e-trading platform internals — pricers, risk engines, scenario analytics, strategist tooling — typically in C++ or Java with substantial finance-DSL or pricing-library experience. None of those specific artefacts exist. The closest signal is Aurix's quant-relevant statistical awareness (selection-bias recognition, three-variant LP framing, Bailey/de Prado Deflated Sharpe acknowledgement) plus Nyquestro's matching engine, but neither is in the e-trading-platform / strategist-tooling shape.

**Supporting evidence**: **Aurix** — pre-trade analytics infrastructure (LP backtester is the closest analogue to a pricing/risk tool); multi-asset reference series across DefiLlama + FRED + beaconcha.in (the cross-asset benchmark mindset transfers); statistical awareness around selection bias. **Nyquestro** — deterministic matching engine + HDR-histogram per-op latency tracking transfers to e-trading-platform internals where execution-quality measurement matters. **Cernio** — production-shaped multi-layer Rust system with strict downward dependency direction; demonstrates the architect-and-ship competence bank-strats roles read for. **NeuroDrive** — handwritten optimisation stack (PPO from scratch + custom biology-inspired learner) shows the from-primitives modelling-from-scratch mindset bank quant-strat roles value, even though the domain doesn't directly transfer.

**Strongest cross-cutting skills**: Quantitative / DeFi finance [Comfortable] (concepts-domains.md), Reproducibility / determinism engineering [Comfortable] (concepts-domains.md), Rust [Proficient] (languages.md), C++ [Familiar] (languages.md), Documentation-first iteration [Proficient] (concepts-domains.md), Test-driven validation [Comfortable] (concepts-domains.md), Honesty-contracted analytics / statistical rigour [Comfortable] (concepts-domains.md).

**Gaps for this lane**: No e-trading platform internals experience (no production order router, no smart-order routing, no fix-engine work); no production pricing-library work (no Black-Scholes / SABR / local-vol surfaces shipped); no Greeks / risk-engine implementation; no scenario / what-if analytics; no FX / rates / credit / equities production experience; no kdb+/q (load-bearing at GS, JPM, Citi e-trading); no OCaml (Jane Street Strats); no Slang / cpp DSLs used at Citadel-style; no actual e-FX / e-rates work; C++ depth caps at Familiar; no formal degree in mathematical finance or financial engineering (the 2:2 from York is structurally below the credential bar at most bank-strats grad programmes — non-closable). The lane-fit is mostly inherited from the systems-engineering signal (Cernio, Nyquestro, Aurix together demonstrate ship-it discipline + finance-adjacent comfort) rather than from direct lane-specific evidence.

---

## systems-infra — Systems / Compilers / Databases / Distributed Infrastructure

**Pinnacle evidence**: **Cernio** — production-shaped multi-layer Rust system (lib + binary + CLI pipeline + 26-file Ratatui TUI + embedded axum web server + AI orchestration layer) over a single SQLite WAL store with strict downward dependency direction, clean inter-layer contracts (SQLite as the typed boundary), 346-test integration suite surfacing three silent production bugs through test investment, 9 native Claude Code skills, 6 ATS provider integrations with shared retry infrastructure. The closest pinnacle-shaped artefact in the portfolio — a fully-shipped, production-tested, multi-layer Rust system with deliberate architectural decisions documented.

**Supporting evidence**: **Image Browser** — SQLite-WAL performance engineering (writer + read-only secondary connection pattern closing a real 22s freeze; `BEGIN IMMEDIATE` per-batch; manual `checkpoint_passive`; `bytemuck::cast_slice` replacing 3 unsafe blocks; partial-sort `select_nth_unstable_by` for 2.53× speedup); production-grade self-observability (PerfLayer + 12 diagnostics + on-exit Stall Analysis report); lock-free single-flight via AtomicBool + RAII guard. **Nyquestro** — strictly layered crate with zero import cycles; backpressure as structural property; safe-Rust-only systems engineering. **Aurix** — production SQLite topology (writer-thread + reader-pool under WAL with tokio-driven `wal_checkpoint(TRUNCATE)`); forward-only `refinery` migrations; idempotency keyed on domain-natural composites. **Xyntra** — typed compiler IR layer (newtype primitives, HashMap-backed graph with monotonic u32 IDs, inputs-only DAG, four-category error taxonomy scaffolded for downstream pipeline stages). **Chrona** — content-addressed VCS internals design (planned, partial scaffold built). **Performance Profiler** — live IL injection via MonoMod/Cecil; lock-free producer/single-consumer queue (276 ns/op); modular subsystem design via single-file extension points. **Zyphos** — raw TCP socket programming + thread-per-connection model + hand-written HTTP/1.1 parser with strict module boundaries. **Open Source Contributions** — backend-generic tensor container fix to burn (PR #4938, +291/-50) closes two long-standing issues; tokio loom/miri prerequisite knowledge.

**Strongest cross-cutting skills**: Rust [Proficient] (languages.md), SQLite (WAL mode) [Proficient] (engines-runtimes.md), Lock-free concurrency / safe-Rust systems [Comfortable] (concepts-domains.md), Tokio [Proficient] (frameworks.md), Modular subsystem design / single-file extension points [Comfortable] (concepts-domains.md), Backpressure as structural property [Comfortable] (concepts-domains.md), Profiling-first engineering culture [Comfortable] (concepts-domains.md), Test-driven validation [Comfortable] (concepts-domains.md), Compiler / IR design [Familiar] (concepts-domains.md), Network protocols / HTTP server engineering [Familiar] (concepts-domains.md).

**Gaps for this lane**: No distributed-database tenure (YugabyteDB / CockroachDB / TiDB / Cloud Spanner / Iceberg / Trino — single-node SQLite work doesn't transfer); no kernel-bypass / async-OS-primitives production work (io_uring / AF_XDP / epoll-from-scratch — Zyphos uses naive `thread::spawn` per connection); no production distributed-systems work (no Raft / Paxos / VSR implementation — Nyquestro V2 proposes VSR but is roadmap); no production compiler work (Xyntra validator is `todo!()` stubs); no LLVM / MLIR / Cranelift contributions; no production storage-engine work (Bw-tree / LSM tree implementation); no production search-engine work; no SIMD-heavy kernel work; no operating-system / hypervisor work. The lane-fit is best-of-the-portfolio because Rust systems work is the spine of every active project, but the cap is "shipped solo systems" vs "shipped distributed-infrastructure at production scale".

---

## devtools — Developer Tools / Developer Experience

**Pinnacle evidence**: **Cernio** — local-first, conversational job-discovery engine combining 6 ATS provider fetchers + 6 CLI commands + 9 native Claude Code skills + 26-file Ratatui TUI + embedded axum web UI + `cernio snap` headless-Chrome visual-debug CLI; explicitly built around the principle that "scripts handle volume and AI handles judgment"; 290KB of skill reference documentation as the quality bar; obligation-anchored over exhortation-anchored skill design. A developer-tools artefact for the developer himself; the closest pinnacle-shaped devtools project in the portfolio.

**Supporting evidence**: **Performance Profiler** — developer-tools artefact for game-mod developers (per-tick CPU/allocation attribution to individual mods); hand-rolled HTTP server + browser SPA replacing the ~5,500-line in-game overlay after five iterations hit the same usability wall; honesty-contracted analytics design with three-dimensional confidence/evidence/baseline badges. **Open Source Contributions** — the `scout-issues` skill is a developer-tools artefact (parallel-agent OSS issue scouting for one-developer's contribution programme); umbrella-folder architecture with whitelist-style `.gitignore` and from-root orchestration enables zero-config clone-as-sibling workflow. **Tessarix** — interactive learning substrate (developer-tools artefact for self-education) with MDX content layer + LLM-integrated assessment + tier inclusion semantics; parallel-agent dispatch pattern (7 agents in one day producing 44 widgets) as a content-production tool. **Image Browser** — opt-in `--profiling` CLI flag with on-exit `report.md` (developer-tools artefact for the project itself).

**Strongest cross-cutting skills**: Documentation-first iteration / context-as-infrastructure [Proficient] (concepts-domains.md), Claude Code skills (native) [Proficient] (tools-platforms.md), Multi-agent / worktree-isolated subagents [Comfortable] (tools-platforms.md), TUI infographics rendering (custom widgets) [Comfortable] (tools-platforms.md), Profiling-first engineering culture [Comfortable] (concepts-domains.md), Modular subsystem design / single-file extension points [Comfortable] (concepts-domains.md), Self-driven verification (visual + behavioural) — section in methodologies-soft.md, Rust [Proficient] (languages.md), Tauri 2 [Proficient] (frameworks.md), Ratatui [Comfortable] (frameworks.md).

**Gaps for this lane**: No production-shaped IDE / editor / extension work (no VSCode extension shipped, no LSP server implementation, no Tree-sitter contributions); no CLI framework / shell tooling at production scale (no shipped `clap`-based CLI used by external users); no build-system work (Bazel / Cargo plugin contributions); no language-server work (no LSP implementations); no debugger / DAP-protocol work; no static-analysis / linter / formatter contributions; no developer-platform / internal-developer-experience experience at any scale; no public Cargo crates with meaningful downloads; no public NPM packages; no developer-community presence (no blog, no public conference talks, no popular tutorials). Strongest devtools signal is the OSS-contribution scout skill + the per-project devtools-for-self pattern, but the lane normally expects either external user count or contributions to widely-used developer tools — both absent.

---

## fintech — Payments / Neobanks / Non-Bank Fintech

**Pinnacle evidence**: No pinnacle evidence in current portfolio. Fintech roles (Stripe, Wise, Monzo, Revolut, Mastercard, Tradeweb) typically expect production payments infrastructure (PSD2 / SCA / 3DS / Open Banking integration), distributed-transaction handling, ledger-and-double-entry-accounting code, KYC/AML, regulatory-compliance-aware system design, or scaled customer-facing financial product engineering. None of those specific artefacts exist.

**Supporting evidence**: **Aurix** — finance-domain engineering (clean-room V3 math, LP backtest, multi-asset benchmark series); demonstrates comfort with finance-adjacent quantitative work and statistical awareness, though it's DeFi-shaped rather than fintech-shaped. **Cernio** — production-shaped multi-layer Rust system with strict downward dependency direction over SQLite; transfers as evidence of architect-and-ship-a-system competence relevant to fintech back-end engineering. **Nyquestro** — deterministic event-driven systems engineering + per-op observability; transfers to payment-event-processing shapes. **Performance Profiler** — production crash-safety engineering (four-layer stack: WAL → NDJSON journal → 3 rotating backups → quarantine), single writer thread with `Interlocked` queue-depth tracking; transfers to ledger-grade durability semantics. **Image Browser** — typed IPC error contract (ApiError discriminated union with `From` impls); transfers to typed-error-contract design in payments systems.

**Strongest cross-cutting skills**: Rust [Proficient] (languages.md), TypeScript [Comfortable] (languages.md), SQLite (WAL mode) [Proficient] (engines-runtimes.md), Tokio [Proficient] (frameworks.md), Modular subsystem design / single-file extension points [Comfortable] (concepts-domains.md), Backpressure as structural property [Comfortable] (concepts-domains.md), Reproducibility / determinism engineering [Comfortable] (concepts-domains.md), Test-driven validation [Comfortable] (concepts-domains.md), Documentation-first iteration [Proficient] (concepts-domains.md).

**Gaps for this lane**: No production payments-rail experience (no PSD2 / SCA / 3DS / Open Banking integration); no production ledger / double-entry accounting code (Stripe / Wise expect this); no KYC / AML / compliance-pipeline experience; no card-issuing / acquiring infrastructure (Mastercard / Visa scheme integration); no payment-orchestration platform experience; no ISO 20022 / SWIFT / FIX-for-payments experience; no production fintech mobile / customer-facing product engineering (Monzo / Revolut / Wise expect this); no PCI DSS-aware system design; no fraud-detection production work (the AsteroidsAI credit-card-fraud detection note in NeuroDrive is teaching-scale only); no real-time payments / instant-settlement infrastructure. The lane-fit is inherited from general systems-engineering signal rather than direct fintech-specific evidence; pinnacle roles will read the portfolio as "strong systems Rust + DeFi-adjacent" and cap at A-stretch rather than S unless the role specifically prizes those properties (e.g. a fintech infra platform team valuing local-first / typed-error / observability discipline over payment-domain depth).
