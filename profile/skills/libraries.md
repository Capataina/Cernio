---
title: Libraries
parent: profile/skills/
last_updated: 2026-05-29
---

## Libraries

| Library | Proficiency | Evidence |
|---------|-------------|----------|
| rusqlite | Comfortable | Cernio (bundled SQLite with WAL mode enabled on every connection open; no system dependency). Image Browser (`bundled` feature for reproducible cross-machine builds without system libsqlite3 dependence; `bytemuck::cast_slice` for safe BLOB casts replacing 3 prior `unsafe` sites in audit `0bdb5f4`; `BEGIN IMMEDIATE` for batched encoder writes with manual `checkpoint_passive` between batches replacing per-row autocommit that produced multi-second WAL stalls). |
| serde / serde_json | Comfortable | Universally used across every Rust project. Cernio (TOML deserialisation for preferences, JSON for ATS responses + provider-specific `ats_extra` payloads, `application_packages.answers` JSON). NeuroDrive (custom struct serialisation for `CompactRunExport`, `BrainUpdateRecord`, `PpoUpdateRecord`, `EpisodeRecord`; `#[serde(default)]` on M6 additions for pre-M6 JSON back-compat). Nyquestro (`#[serde(tag = "kind", rename_all = "snake_case")]` tagged enums for ~19 `TelemetryEvent` variants designed for `cat \| jq` analysis). Image Browser (`#[serde(tag = "kind", content = "details")]` discriminated union for `ApiError` typed IPC wire format with frontend mirror). Aurix (`serde(rename_all = "camelCase")` as the only wire-level Rust↔TS bridge for 19 IPC commands). |
| reqwest | Comfortable | Cernio (shared `http::build_client()` plus `ats::common::get_with_retry` / `post_json_with_retry` helpers wrapping it with 500ms-×-attempt exponential backoff on timeout/connection/request errors; non-retryable 4xx returns immediately). Aurix (`reqwest 0.12` with `rustls-tls` feature to avoid system OpenSSL dependency simplifying cross-platform Tauri builds; `Lazy<reqwest::Client>` hoist post-audit). Tessarix (`reqwest 0.12` with `rustls-tls`/`json`/`stream` for Ollama HTTP traffic + SSE streaming via `futures-util::StreamExt`). |
| chrono | Comfortable | Aurix (`chrono 0.4` with `clock, serde` features for datetime handling). Cernio (chrono format `%Y-%m-%dT%H:%M:%S` vs SQLite's `%Y-%m-%d %H:%M:%S` was the cross-cutting bug fixed across 7 files in commit `50359b13`). Nyquestro (`chrono 0.4` for human-readable time conversion in telemetry, not used in the matching loop). Zyphos (`chrono 0.4` for RFC 1123 `Date` header via `Utc::now().format("%a, %d %b %Y %H:%M:%S GMT")` and `/time` route body via `Local::now()`). |
| thiserror | Comfortable | Vynapse (`thiserror = "2.0.12"` for error type derivation in `vynapse-common`, `Display`/`Error` impls only). Nyquestro (14-variant `NyquestroError` with `#[error("…")]` format strings embedding field values for readable test assertions). Aurix (per-module error enums rather than a grand unified type, adapter errors wrapping `EthereumRpcError` with `#[from]` and `#[error(transparent)]`). |
| rand / rand_distr / rand_chacha | Comfortable | NeuroDrive (`StdRng` seeded per-brain, `Normal(0, σ)` for synapse-weight init, `Uniform` for spawn-point + sprout-candidate sampling). Vynapse (`rand = "0.9.1"` + `rand_distr` for RNG + Gaussian-distribution sampling for mutation/tournament-selection/weight-init; deterministic seeding is an open Milestone 1 gap). Nyquestro (`ChaCha8Rng` as the only randomness source in the synthetic simulator — two `MarketSimulator::new(_, 42)` instances produce byte-identical action streams, pinned by `run_twice_identical_sequence_identical_output`). |
| hdrhistogram | Familiar | Nyquestro (`hdrhistogram 7.5` per-`Op` histograms with 1ns lower bound, 1 hour upper bound, 3 significant figures, `auto(true)` autoresize; the dashboard surfaces p50 / p95 / p99 / p999 / p9999 / max / mean because "a candidate showing only p50 gets dismissed politely"). |
| num-bigint | Familiar | Aurix (`num-bigint 0.4` for Q64.96 fixed-point arithmetic in the clean-room Uniswap V3 port; chosen over `ruint::U256` because the "no `ethers-rs`, clean-room port from Solidity" framing reads stronger when the underlying types are general-purpose Rust primitives; 20 magic constants in `tick_to_sqrt_price_x96` allocated once via `Lazy<[BigUint; 20]>` per `once_cell`). |
| ort (ONNX Runtime Rust bindings) | Familiar | Image Browser (`ort = 2.0.0-rc.10` with shared M2-tuned `Session` builder factory `build_tuned_session` — `Level3 + intra_threads(4) + inter_threads(1)` with Phase 12 dynamic `intra = N / enabled_encoders`; CoreML disabled for all encoders on macOS because of transformer-op runtime errors; CUDA target-gated on non-macOS with CPU fallback). |
| HuggingFace tokenizers | Familiar | Image Browser (`tokenizers = "0.22.2"` handling BPE for CLIP — vocab 49 152, max 77 tokens, pad id 49407 — and SentencePiece for SigLIP-2 — Gemma vocab 256k, max 64 tokens, no attention_mask — uniformly via `tokenizer.json`; replaced a previous pure-Rust hand-rolled WordPiece multilingual encoder). |
| bytemuck | Familiar | Image Browser (`bytemuck::cast_slice` for safe zero-copy alignment-checked `&[f32] ↔ &[u8]` views; audit `0bdb5f4` consolidated this pattern, extracting 3 prior `unsafe { slice::from_raw_parts(...) }` sites). |
| crossterm | Familiar | Cernio (terminal backend for ratatui with mouse support including Ctrl+click multi-select and Shift+click range). Nyquestro (`crossterm 0.28` for raw-mode setup, input event polling at 10ms cadence, terminal restore). |
| tokio-tungstenite | Familiar | Nyquestro (`tokio-tungstenite 0.24` with `native-tls` feature for the Coinbase Advanced Trade WebSocket; `native-tls` chosen over `rustls-tls` after `rustls` panicked at startup with "Could not automatically determine the process-level CryptoProvider"; `native-tls` yields macOS Security.framework trust store for free). |
| tokio-rusqlite + r2d2 + refinery | Familiar | Aurix (`tokio-rusqlite 0.5` async SQLite writer on one dedicated thread serialising mutations; `r2d2 0.8 + r2d2_sqlite 0.24` fixed-size reader connection pool ~4 connections; `refinery 0.8` forward-only embedded SQL migrations running on every `Storage::open`). |
| fast_image_resize + jpeg-decoder + image-rs | Familiar | Image Browser (`fast_image_resize 6.x` NEON-optimised Lanczos3 final downsample; `jpeg-decoder::Decoder::scale()` for native scaled IDCT at 1/8, 1/4, 1/2 factor; together ~5–10× faster than the prior `image-rs` Lanczos3 default — on a 2000-image library thumbnail-phase time dropped from ~150–250s to ~30–50s; `image-rs` retained as fallback for non-JPEG decode). |
| rayon | Familiar | Image Browser (`par_iter` parallelism for the thumbnail phase across all available cores). |
| notify-debouncer-mini | Familiar | Image Browser (filesystem watcher with 5-second debounce window; event handler calls `indexing::try_spawn_pipeline`; single-flight rejects rapid bursts silently). |
| tracing + tracing-subscriber | Familiar | Image Browser (structured spans + log levels; `PerfLayer` is added only when `--profiling` is enabled; span-name convention with `ipc.` / `pipeline.` / `cosine.` / `model_download.` / `watcher.` prefixes for cross-phase grouping). Aurix (referenced as part of the diagnostic posture; per-system tracing across LP pipeline). |
| bincode | Familiar | Image Browser (persistent `cosine_cache.bin` serialisation; mtime freshness check against DB; corrupted cache silently falls through to `populate_from_db`). |
| sysinfo | Familiar | Image Browser (`sysinfo 0.32` for the 1 Hz RSS / CPU% sampler feeding the profiling Resource Trends markdown report section). |
| dirs | Familiar | Image Browser (platform-default app-data directory resolution). Nyquestro (`dirs 5` for platform-canonical data directory lookup for the JSONL flight recorder — `~/Library/Application Support/Nyquestro/`, `~/.local/share/nyquestro/`, `%LOCALAPPDATA%\Nyquestro\`). |
| ureq | Familiar | Image Browser (first-launch HuggingFace model downloads — HEAD preflight skip-if-exists with matching size, chunked GET with progress callback, atomic `.part` + rename to final name). |
| Apple Accelerate (FFI) | Familiar | NeuroDrive (`Accelerate` framework called via FFI for AMX — Apple Matrix coprocessor — mat-mat operations in the PPO update path, behind a dual-backend abstraction with SIMD fallback that still passes `force-scalar` test builds). |
| futures-util | Familiar | Nyquestro (`SinkExt`/`StreamExt` for the WebSocket split). Tessarix (`futures-util 0.3 StreamExt` for SSE chunk iteration in the LLM streaming path). |
| once_cell | Familiar | Aurix (`Lazy<BigUint>` constants for the 20 Q128.128 magic constants in `tick_to_sqrt_price_x96`; `Lazy<reqwest::Client>` hoist post-audit). |
| dotenvy | Familiar | Aurix (`.env` file loading with fallback path `../.env`; `ENVIRONMENT_BOOTSTRAP: Once`-guarded so the resolved RPC URL parses exactly once). |
| Catch2 (C++) | Familiar | Chrona (Catch2 v3.5.0 via CMake `FetchContent`; `Catch2::Catch2WithMain` provides `main()`; 11 working `TEST_CASE`s exhaustively covering the CLI parse-rule matrix using the const-correctness `const_cast<char**>` idiom and `ostringstream` capture for output assertions). |
| NumPy | Familiar | AsteroidsAI (NumPy-only for all evolutionary tensor work — `FeedforwardPolicy` MLP, genome unpacking, ES diagonal-CMA covariance update; per-thread `random.Random(random_seed)` to prevent global-state contamination across concurrent `ThreadPoolExecutor` rollouts). |
| PyTorch | Familiar | AsteroidsAI GNN-SAC (twin critics with Polyak averaging at `tau=0.005`, auto-entropy temperature toward `TARGET_ENTROPY=-3.0`, asymmetric LR — actor 3e-4 vs critic 1e-4 — for stability under GNN-noisy embeddings, Huber TD loss with `delta=1.0`, AGC adaptive gradient clipping with `clip_factor=0.01` on top of global grad-norm clipping; graph-native replay buffer storing variable-size graph transitions). |
| PyTorch Geometric | Familiar | AsteroidsAI (`torch_geometric.nn.GATv2Conv` × 2 layers / 64 hidden / 4 attention heads as the GNN backbone over bipartite asteroid → player graph; backend deps `torch_scatter`/`torch_sparse` required at install but unpinned — flagged as revival-time friction). |
| proptest + assert_cmd + tempfile + predicates | Familiar | Cernio (`assert_cmd` CLI integration tests via `Command::cargo_bin("cernio")` spawning the real binary against a tempdir DB through `CERNIO_DB_PATH`; 16 CLI tests; `proptest` listed in the stack table; `tempfile` for per-test tempdirs; `predicates` for assertions). Aurix (`proptest 1` dev dependency for property-based testing of math primitives). |

---
