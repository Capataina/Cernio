---
title: Libraries
parent: Skills
last_updated: 2026-05-31
---

# Libraries

> Discrete libraries providing functionality the program calls into. Distinct from Frameworks.

| Library | Proficiency | Evidence |
|---------|-------------|----------|
| rusqlite | Comfortable | **Cernio**: bundled; WAL on every open; `test_support::open_in_memory_db()` workhorse fixture; 5 tables / 6 idempotent migrations / 29 inline tests. **Image Browser**: dual writer + read-only secondary connection pattern (`OnceLock<Mutex<Conn>>`); `BEGIN IMMEDIATE` per ~32-row chunk; manual `checkpoint_passive` between encoder batches; `bytemuck::cast_slice` for embedding BLOBs. **Aurix**: bundled feature compiles from source for reproducibility; `tokio-rusqlite 0.5` + `r2d2 0.8` + `r2d2_sqlite 0.24` writer-thread + reader-pool topology. Three projects, distinct topologies. |
| serde / serde_json | Comfortable | **Cernio**: per-provider ATS deserialisation. **Aurix**: `serde(rename_all = "camelCase")` boundary contract across every Rust DTO. **NeuroDrive**: `BrainUpdateRecord`, `CompactRunExport`, `EpisodeTracker`; `#[serde(default)]` for forward/backward-compatible JSON across pre/post-M6 boundary. **Image Browser** `ApiError` `#[serde(tag="kind", content="details")]` discriminated union. **Nyquestro** telemetry JSONL with schema-version `v: 1`. Cross-project default. |
| reqwest | Comfortable | **Cernio** (`src/http.rs`): shared HTTP client with per-request exponential backoff (500ms × attempt). **Aurix** 0.12 with `rustls-tls`: identical cross-platform builds. **Tessarix** 0.12 with `rustls-tls`: `LlmClient` chosen over `native-tls` to avoid OpenSSL build dependencies. Repeated use across HTTP-client domain. |
| tokio-tungstenite | Familiar | **Nyquestro**: `tokio-tungstenite 0.24` with `native-tls` feature for Coinbase Advanced Trade `level2` WebSocket; `native-tls` chosen over `rustls-tls` after CryptoProvider startup panic (gains macOS Security.framework as side-benefit). One project, live exchange WebSocket integration. |
| num-bigint | Comfortable | **Aurix**: 256-bit arithmetic for Q64.96 sqrt-price math; deliberately chosen over `ruint::U256` for clean-room-port narrative; bit-by-bit `tick_to_sqrt_price_x96` port using 20 magic constants precomputed as `Lazy<[BigUint; 20]>`; per-direction rounding policies preserved from Solidity. Single deep project, research-grade arithmetic work. |
| hdrhistogram | Familiar | **Nyquestro**: hdrhistogram 7.5; per-op latency percentiles (p50/p95/p99/p999/p9999/max) with autoresize across `Op::Submit/Match/Cancel`. One project; engineered around its autoresize semantic. |
| thiserror | Comfortable | **Aurix**: per-subsystem typed error enums (`StorageError`, `V3MathError`, `IngestError`, `BacktestError`, `BenchmarkError`). **Nyquestro**: `NyquestroError` with severity-classification method. **Vynapse**: `VynapseError` with three variants + `Result<T>` alias; `thiserror 2.0.12`. **Tessarix** + **Cernio** + **Image Browser**: ApiError patterns. Repeated, idiomatic. |
| chrono | Comfortable | **Aurix** 0.4 (clock, serde): timestamp handling. **Nyquestro**: human-readable timestamp conversion only (not in engine hot path). **Cernio**, **Image Browser**, **Zyphos**: RFC 1123 date formatting (`%a, %d %b %Y %H:%M:%S GMT`) plus local-vs-UTC distinctions. **AsteroidsAI**, multiple. Cross-project default. |
| ort (ONNX Runtime Rust) | Comfortable | **Image Browser**: `ort = 2.0.0-rc.10`; single shared M2-tuned `Session` builder factory in `ort_session.rs::build_tuned_session` with `Level3 + intra_threads(4) + inter_threads(1)`; Phase 12 dynamic `intra_threads(N / enabled_encoders)` for shared M2 P-cluster; CoreML disabled uniformly; CUDA target-gated. Single project, substantial depth — three encoders + two text encoders through one tuned factory. |
| HuggingFace tokenizers | Familiar | **Image Browser**: `tokenizers = "0.22.2"` (HuggingFace Rust crate) — uniform BPE (CLIP, vocab 49,152, max 77) + SentencePiece (SigLIP-2 Gemma vocab 256k, max 64) via shared `tokenizer.json` interface; replaced "pure-Rust no-C-deps" position for ergonomics + correctness. |
| fast_image_resize / jpeg-decoder | Familiar | **Image Browser**: `fast_image_resize 6.x` (NEON-optimised Lanczos3) + `jpeg-decoder 0.3` native scaled IDCT at 1/8, 1/4, 1/2 factor for ~5-10× JPEG thumbnail speedup. Single project, single-purpose. |
| bytemuck | Familiar | **Image Browser**: safe zero-copy `&[f32]` ↔ `&[u8]` casts for embedding BLOB storage; replaces three `unsafe { slice::from_raw_parts(...) }` blocks (audit `0bdb5f4`). Single, deliberate replacement. |
| chromiumoxide | Familiar | **Cernio**: used by both broken autofill scaffold and the working `cernio snap` debug-screenshot CLI; spawns the embedded axum server and drives headless Chrome over web UI tabs. Single project, dual purposes. |
| rand / rand_chacha / rand_distr | Comfortable | **NeuroDrive**: `StdRng` seedable RNG on `BrainBrain`. **Nyquestro**: `ChaCha8Rng` as only randomness source for byte-identical action streams under fixed seed. **Vynapse**: `rand 0.9.1` + `rand_distr` for evolutionary mutation/crossover/selection. **AsteroidsAI**: GA + CMA-ES + NEAT all consume seeded RNG paths. Repeated, with explicit determinism reasoning. |
| LiteDB | Familiar | **Performance Profiler**: LiteDB 5.0.21 (MIT, 510 KB managed DLL packed via `dllReferences = LiteDB` + `lib/LiteDB.dll`); deliberately not 6.0 prerelease; replaces 940 lines of `SessionLogWriter.cs`; two real bugs caught by test wiring (`Pragma("USER_VERSION")` BsonValue typing trap; `Channel.Reader.Count` macOS unsupported). One project, deep production-shaped use. |
| MonoMod / Cecil | Comfortable | **Performance Profiler**: `MonoModHooks.Add` delegate-pair backend (~30 distinct signature families); `new ILHook(target, manipulator, applyByDefault: true)` IL backend (signature-agnostic, ~100% coverage); manipulator rewrites every `ret` to wrap in `try/finally` with `ProbeStack.Enter`/`Leave`; JIT shared-body trap mitigated via `_tmlAssembly` filter (commits `7da4058` enabling, `5725572` filtering one day later). Single project, production-shipping live IL injection. |
| System.Threading.Channels | Familiar | **Performance Profiler**: `Channel.CreateUnbounded` (single-reader, multi-writer) for lock-free producer queue feeding LiteDB writer thread; `Interlocked`-tracked `_approxQueueDepth` because `Channel.Reader.Count` unsupported on .NET 8 / macOS unbounded variant. Single project. |
| torch_geometric | Familiar | **AsteroidsAI**: `GATv2Conv` layers in GNN-SAC backbone; graph-batching primitives in SAC replay path. Single project. |
| PyTorch | Familiar | **AsteroidsAI**: SAC actor/critic networks, autograd for SAC policy gradient. **Open Source Contributions**: PyTorch-weight loader path for burn's perceptual metrics. Limited surface; framework usage rather than custom-op authoring (the tinygrad LSTM operator is the deeper signal). |
| refinery | Familiar | **Aurix**: 0.8 forward-only migrations embedded in binary; `V001__initial.sql` + `V002__multi_asset_headline.sql`. Single project. |
| Catch2 | Familiar | **Chrona**: v3.5.0 via CMake `FetchContent`; `TEST_CASE` + `SECTION`; `Catch2::Catch2WithMain`; 11 working tests in test_cli.cpp. **Xyntra**'s test-suite design pattern shares the same convention. |
| xUnit | Familiar | **Performance Profiler**: `PerformanceProfiler.Tests.csproj` with `Compile Include` + `Link` to pure-logic source so tests stay out of shipped `.tmod`; 10 fixtures including a persistence benchmark asserting 276 ns/op enqueue floor. |
| once_cell | Familiar | **Aurix**: `Lazy<T>` for process-wide constants (Q96 helpers, magic-tick array, reqwest client hoist). Subset of broader Rust pattern usage. |
| maud | Familiar | **Cernio web UI**: type-safe HTML templates; gotcha discovered — `selected=(false)` emits `selected="false"` and browsers treat any presence as truthy; correct conditional-attribute syntax is `selected[bool_expr]`. |
| ECharts | Familiar | **Cernio web UI**: ECharts 5.4; data delivered via JSON islands `<script type="application/json" id="data-<kind>">` written by handlers via `json_island(kind, &value)`; `chart.on('click')` URL navigation. |
| clap | Familiar | **Vynapse** (declared, unused). **NeuroDrive**, **Cernio**, **Nyquestro** CLI surfaces use it. Standard Rust CLI library usage; no advanced subcommand or completion work surfaced. |
| toml | Familiar | **Cernio**: `toml = "0.8"`; `preferences.toml` → typed config structs via `src/config.rs`; loader intentionally lenient guarded by 21 build-time integrity tests. |
