---
title: Programming Languages
parent: Skills
last_updated: 2026-05-31
---

# Programming Languages

> Evidence-anchored. Derived from `profile/projects/*.md` per the `populate-from-lifeos` skills-derivation rubric. Proficiency bands: Proficient | Comfortable | Familiar.

| Language | Proficiency | Evidence (specific projects + what they demonstrate) |
|----------|-------------|------------------------------------------------------|
| Rust | Proficient | Central to 7+ projects across 5+ distinct domains. **Cernio**: ~14k LoC, lib+bin split, 56 source files, Tokio async pipeline + axum web layer + 26-file Ratatui TUI + 6 ATS fetchers; 346 tests. **Image Browser**: 28 backend files; `Arc`/`Mutex`/`AtomicBool`/`OnceLock`/RAII Drop guards; writer + read-only secondary SQLite-WAL connection pattern closing a real 22s freeze; `bytemuck::cast_slice` replacing 3 `unsafe` blocks; partial-sort `select_nth_unstable_by` for 2.53× top-K speedup. **NeuroDrive**: 80 `.rs` files (~660 KB); Bevy ECS; ZST marker components for compile-time partitioning; flat row-major weight buffers; `mem::swap`; 426→2 stutter reduction. **Aurix**: ~10,500 LoC across 79 files; clean-room V3 math port on `BigUint`; `tokio_rusqlite` writer-thread + `r2d2_sqlite` reader-pool under WAL; `thiserror` per subsystem; `once_cell::Lazy` for process-wide constant precompute. **Nyquestro**: safe-Rust-only (zero `unsafe`), edition 2024, ten-module strict layered crate, BTreeMap-keyed order book, four-phase deterministic `submit_limit`. **Vynapse**: 4-crate workspace (`common→math→core→cli`) with `Cargo.toml`-enforced dependency direction; generic `EvolutionaryTrainer<G,M,C,F,S>` over 5 trait params; safe Rust only. **Xyntra**: typed IR primitives, Rust 2024, newtype wrappers, `unsafe`-free. Cross-domain transfer (TUI + DB + game engine + ML + DeFi + HFT-style + desktop apps) lifts to Proficient. |
| TypeScript | Comfortable | **Image Browser**: 33 TS files; React 19 + TanStack Query 5 + Vite 7; `vite-plugin-pages`; typed `ApiError` discriminated-union mirror; `useUserPreferences`/`useIndexingProgress` hooks; `perfInvoke<T>` IPC wrapper. **Aurix**: ~9,000 LoC across 45 `.tsx` + 18 `.ts` files; multi-tab routing; `usePersistedState` shape-merge; 217-line `types.ts` mirroring Rust DTOs; telemetry hub (397 lines). **Tessarix**: 79 `.tsx` + 20 `.ts` files; React 19 + MDX + hash router + ErrorBoundary + multi-provider composition; `IntersectionObserver`-driven active-section tracking. Three substantial production-shaped frontends; no compiler/systems work. |
| Python | Comfortable | **AsteroidsAI**: 141 Python files (~806 KB); 4 optimisation paradigms implemented (GA, diagonal CMA-ES, NEAT, GNN-SAC); shared 45 KB evaluator parameterised by `agent_factory`; PyTorch + `torch_geometric` (GATv2Conv); `ThreadPoolExecutor`-parallel headless rollouts. **Consilium**: Python 3.11+, ~106 KB across 23 files; frozen dataclasses, tolerant JSON type-coercion; Textual TUI with `@work` background-worker pattern; LangChain provider abstraction. **Open Source Contributions**: tinygrad PRs #15453 (+78 tokenised lines) and #16119 (14-line minimum LSTM) to `tinygrad/nn/onnx.py`. Numeric / ML / orchestration use across multiple projects. |
| C++ | Familiar | **Chrona**: C++20, CMake 3.20+; ~20.8 KB of source + ~15.9 KB tests; `enum class`, `std::optional`, `std::filesystem`; Catch2 FetchContent; `argc/argv` manual CLI parser; value-based error model. M0 partial, dormant. **Tectra**: C++20, `-Wall -Wextra -Wpedantic -Werror` from day one; ~80 lines (Clock interface with `RealClock` via `steady_clock`, `VirtualClock` with `advance`/`set_time`, `LogSeverity` enum); dormant after 6 days of work. Both projects design-heavy, implementation-light; band capped by build status. |
| C# / .NET 8 | Comfortable | **Performance Profiler**: ~39,121 production LoC across 264 files; per-file `#nullable enable`; file-scoped namespaces (C# 10+); zero-allocation hot path (`Stopwatch.GetTimestamp()` static reads, pre-allocated arrays, `BoolIndex` for O(1) buff diff); `Channel.CreateUnbounded` SPSC queue (276 ns/op enqueue); single writer thread with `Interlocked`-tracked queue depth; hand-rolled ~250-line loopback HTTP server on `TcpListener`; live IL injection via MonoMod / Cecil rewriting every `ret` instruction. Single-project depth; ships v0.12 across 148 commits in three days. |
| MDX | Familiar | **Tessarix**: 9 lesson MDX files (~244 KB) mixing markdown prose with React widget components and KaTeX math; `@mdx-js/rollup` plugin chain with `enforce: "pre"` ordering; `remark-math` → `rehype-katex`; `providerImportSource: "@mdx-js/react"` for component overrides. Content-pipeline configuration depth, not language depth. |
| SQL | Familiar | Used in **Cernio** (5 tables + 6 migrations, idempotent rebuild pattern for `ALTER CHECK`, INSERT OR IGNORE dedup), **Aurix** (V001 + V002 refinery migrations; composite-key `INSERT OR IGNORE` vs `INSERT OR REPLACE` for provider-revisable series; TEXT-decimal big-integer encoding for uint160/uint128/int256), **Image Browser** (4 idempotent ALTER-TABLE migrations + `embedding_pipeline_version` meta migration), **Performance Profiler** (LiteDB queries + indexes via `IPersistenceStream`). Schema-design fluency demonstrated; no analytical SQL or distributed-SQL work. |
