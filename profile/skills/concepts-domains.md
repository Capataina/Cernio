---
title: Concepts and Domains
parent: Skills
last_updated: 2026-05-31
---

# Concepts and Domains

> The most important table for grading fit. Domains demonstrated through project work, separate from any specific tool. Concepts transfer across stacks; tools do not.

| Domain | Proficiency | Evidence |
|--------|-------------|----------|
| Local-first architecture | Proficient | **Cernio** (SQLite file, no Docker, no server, no API keys for core system; AI layer runs through Claude Code sessions — no hosted inference). **Image Browser** (only network call is first-launch ~2.5 GB HuggingFace model download; privacy by construction, not configuration). **Aurix** (all computation and storage on-device; no cloud, no telemetry, no server; no paid APIs as architectural constraint; tiered fallback subgraph → Alchemy → public RPC → empty state). **Performance Profiler** (loopback-only `127.0.0.1` bind; zero firewall prompts; no remote-attack surface). **Tessarix** (local LLM via Ollama; Claude API reserved for quality-critical paths). Recurring across 5+ projects with explicit architectural defence in writing. |
| Reinforcement learning | Proficient | **NeuroDrive**: handwritten PPO with clipped surrogate objective and GAE, asymmetric actor-critic (2×64 actor, 2×128 critic), tanh-squashed actions with Jacobian correction, orthogonal initialisation with scale-differentiated heads, PopArt adaptive value normalisation, observation running-stats normalisation, target-KL early stop; zero external ML framework dependency. Plus full **biologically-plausible learning substrate**: three-factor plasticity `Δw = η·M·e` with per-car eligibility traces (`λ=0.992`, τ_e ≈ 2s at 60 Hz), STDP-like causal semantics, raw-reward modulator, biological homeostasis on two cadences, continual-backprop utility tracking with mature-neuron replacement, plateau-triggered neurogenesis. **AsteroidsAI**: GA (BLX-α + Gaussian mutation + tournament), diagonal CMA-ES with target-rate adaptation + antithetic sampling + Pareto rank selection, full NEAT (topology growth + speciation + innovation tracking), Soft Actor-Critic with GNN backbone (twin critics, entropy regularisation, graph-native replay buffer). The strongest single-domain evidence in the portfolio. |
| Lock-free concurrency / safe-Rust systems | Comfortable | **Nyquestro**: safe-Rust-only (zero `unsafe`) deterministic matching engine with `BTreeMap<Px, PriceLevel>` ladders; backpressure as structural property (`SNAPSHOT_LEVEL_CAP=50` + `PER_FRAME_BUDGET=500` two-pronged defence); bounded `sync_channel(8192)` + `try_send` + atomic drop counter for non-blocking telemetry. Pre-lock-free MVP: `BTreeMap`/`VecDeque` foundation deliberately shipped first per D2 (correctness before performance); lock-free order book is roadmap. **Image Browser**: lock-free single-flight via AtomicBool + RAII guard (`RunningGuard` Drop clears flag on success, error, AND panic). **NeuroDrive**: concurrent rollout buffer with deterministic determinism at fixed 60 Hz. **Performance Profiler**: `Channel.CreateUnbounded` SPSC lock-free producer queue (276 ns/op enqueue). Single safe-Rust lock-free production order book remains the cap — the actual lock-free matching engine is roadmap, foundation only is shipped. |
| Market microstructure / limit-order-book engineering | Comfortable | **Nyquestro**: strict price-time priority with snapshot-and-compare top-of-book quote semantics, four-phase deterministic `submit_limit` (snapshot pre-state → aggressive matching → self-match handling → rest aggressor); microstructure inspection surface (`microprice()` volume-weighted between best bid/ask, `ofi(n)` order-flow imbalance, `spread_cents()`, `depth(n)`, `top_n_bids/asks(n)`); Ornstein-Uhlenbeck mid-walk + Poisson per-side arrivals + log-normal sizes; live Coinbase `level2` WebSocket integration; idempotent retraction map keyed by `(Symbol, Side, Px)`; `Symbol(u64)` 8-byte ASCII pack for multi-instrument routing with O(1) hash/order. Single project, deep, deliberately incremental. |
| HFT-style observability / tail latency | Comfortable | **Nyquestro**: per-op HDR-histogram latency percentiles (p50/p95/p99/p999/p9999/max) across `Op::Submit/Match/Cancel`; `WindowedCounter` with lazy pruning over rolling 1s/10s/1min/5min; schema-versioned JSONL flight recorder with truncate-on-startup; health-dot system from slow-frame counts + p99 thresholds. **Image Browser**: PerfLayer + 12 named diagnostics + 1 Hz RSS/CPU sampler + on-exit `report.md` with Stall Analysis + Resource Trends. **Performance Profiler**: median + MAD outlier spike detection; stall classifier with `MainThreadFreeze` cause; stall clustering aggregates "what the player perceived as one freeze"; honesty-contracted analytics with `Confidence × EvidenceScope × BaselineKind` orthogonality + `PValueAdjusted`-gated promotion. Three projects with production-grade self-observability. |
| Quantitative / DeFi finance | Comfortable | **Aurix**: clean-room port of Uniswap V3 mathematics stack (TickMath / FullMath / LiquidityAmounts / SqrtPriceMath) onto `num-bigint::BigUint`; Q64.96 sqrt-price encoding; 20 magic constants for tick-to-sqrt-price exponential precomputed as `Lazy<[BigUint; 20]>`; per-direction rounding policy preserved from Solidity; in-range fee share calculation; V3-concentrated impermanent-loss closed forms; per-swap LP backtest engine with LVR discrete approximation per Milionis-Moallemi-Roughgarden; rebalance rules (Static / Schedule / OutOfRange) with explicit recentring + gas-cost accounting; three audit-applied perf wins (pre-parse `ParsedSwap` hoist eliminating ~40k allocations per 10k-swap run; loop-invariant `HoldOnlyEvaluator`; incremental `fees_usd_acc` collapsing O(N²) → O(N) with documented ULP-drift bound ~2e-11 USD); multi-asset reference series (Aave V3, Compound V3, Lido, 3M T-bill, S&P 500, gold LBMA, ETH.STORE, V2 LP synthetic, HODL); adaptive-tercile vol-regime classifier replacing fixed thresholds; three-variant LP (Best / Naive / Median) framing to defeat selection-bias readings. Statistical awareness — explicit recognition that "best-cell LP wins" is selection-bias-inflated; Bailey/de Prado Deflated Sharpe acknowledged. |
| Cross-chain DeFi / on-chain decoding | Familiar | **Aurix**: 5 chains (Ethereum / Arbitrum / Optimism / Base / Polygon) × 3 protocols (Uniswap V3 + Sushi V3 + Pancake V3) with per-chain `ChainConfig`; three-tier free-data fallback architecture (subgraph → Alchemy → public RPC → empty); Ethereum archive log decoders with two's-complement int24 sign-extension; event-signature topic[0] keccak verification. Single project, cross-chain scope. |
| Multi-encoder rank-fusion retrieval | Comfortable | **Image Browser**: Reciprocal Rank Fusion (Cormack 2009) `fused_score(p) = Σ over enabled encoders e of 1 / (k_rrf + rank_e(p))` with canonical `k_rrf = 60`; image-image fusion over three encoders (CLIP + SigLIP-2 + DINOv2), text-image fusion over two (CLIP + SigLIP-2 — DINOv2 has no text branch); per-encoder evidence in diagnostics; lazy per-encoder caches; non-destructive toggle semantics; 6 RRF unit tests pin the contract. |
| Computer vision / vision-language models | Comfortable | **Image Browser**: CLIP ViT-B/32 OpenAI English (512-d), DINOv2-Base (768-d, image-only), SigLIP-2 Base 256 (768-d shared text+image); cross-encoder preprocessing discipline (CLIP-native mean/std replacing biased ImageNet stats; bicubic-shortest-edge-224 + center-crop replacing `resize_exact`; L2-normalize on output; SigLIP-2 `[-1, 1]` range with exact-square 256×256 + Gemma SentencePiece 64-token; DINOv2 CLS-token from `last_hidden_state[:,0,:]`); pipeline-versioned embedding migration (currently v4, bumped 2026-04-26). **Open Source Contributions**: A-FINE perceptual metric for burn — CLIP ViT inlined backbone + five evaluator heads + PyTorch-weight loader; +1864 lines across 10 files, approved by maintainer. |
| LLM systems / prompt engineering | Comfortable | **Tessarix**: empirical model selection across 4 local models with documented failure modes (qwen2.5:3b factor-of-4 hallucinations + Chinese-token leakage; gemma2:2b architectural symbolic-hallucination issue); prompt discipline as load-bearing (system/user split, persona priming, bounded output length, system-prompt grounding rule, temperature 0.2 / top_p 0.9, JSON-schema mode for structured output, token caps); DOM-based context injection (lesson context scraped at request time, cap ~8KB) rather than MDX source. **Consilium**: multi-agent debate orchestration with structured 8-key `DebateStateSnapshot` JSON state replacing free-form prose; anti-evaluative prompt contract with explicit banned-word lists (`dominant`, `robust`, `precise`, `failed to`, ...); peer-output isolation invariant (agents see only their own previous response + structured state); deterministic fallback via first-sentence/first-paragraph extraction; provider-agnostic factory + thin-adapter pattern over Ollama + Google. Both projects, distinct architectural approaches. |
| Reproducibility / determinism engineering | Comfortable | **NeuroDrive**: bit-deterministic kinematic physics at fixed 60 Hz; RNG seeded via `config.rng_seed` when present; explicit anti-pattern review for sources of nondeterminism. **Nyquestro**: determinism test `run_twice_identical_sequence_identical_output` pins byte-identical engine output across runs; `Ts::now()` never called in matching loop; ChaCha8-deterministic synthetic flow. **Tectra**: `Clock` interface with `RealClock` (`steady_clock` for monotonicity vs NTP-induced backwards jumps) + `VirtualClock` (`advance` / `set_time` accepting earlier values for replay seeking); `const Clock&` for services so they cannot mutate virtual time. **Aurix**: `config_hash` (deterministic SHA over backtest config) keys cache so React StrictMode's intentional double-mount hits cache on second invocation; reproducibility-by-construction in `MockArchiveSource` (deterministic sinusoidal swap walks). Recurring pattern with explicit type-level enforcement. |
| Backpressure as structural property | Comfortable | **Nyquestro**: `SNAPSHOT_LEVEL_CAP=50` at wire boundary + `PER_FRAME_BUDGET=500` at dispatch boundary makes dashboard non-freezable under any feed load; `sync_channel(8192)` + `try_send` + atomic drop counter for non-blocking telemetry. **Image Browser**: writer + reader-only secondary connection closes 22s freeze. **Performance Profiler**: single writer thread + bounded queue + 64-op batches + 60s checkpoint cadence. Pattern with documented "structural, not configuration" framing. |
| Modular subsystem design / single-file extension points | Comfortable | **Performance Profiler**: `IPersistenceStream` + `StreamRegistry` (one file + one registry line per new collection); `IDataStream` + `DataRegistry.Shared` (one class + one register call per new stream); validated by adding 3 new collections in v0.4 + 17 new streams in v0.12 without modifying any pre-existing stream. **Cernio**: per-provider ATS fetcher pattern (`src/ats/<provider>.rs` files share `common.rs` retry/slug helpers but each module owns its quirks); 6 native skills follow the same authoring contract; six pipeline commands isomorphic in shape. **Aurix**: `ArchiveSource` polymorphic trait with `fetch_logs`, `latest_finalized_block`, `block_gas_price_gwei`, `pool_metadata`. Recurring extension-point discipline. |
| Test-driven validation / property tests / parity tests | Comfortable | **Cernio**: 346 tests (273 inline + 73 integration); 21 build-time `preferences_integrity.rs` invariants; surfaced three silent production bugs through test investment (two data-loss bugs session 9, Workday UK-filter bypass session 10, timestamp-format mismatch session 11). **Image Browser**: 125 cargo lib tests + 62 vitest tests; `cosine_topk_partial_sort_diagnostic.rs` integration test for 2.53× speedup verification. **Aurix**: 139 backend tests + `proptest` (dev). **NeuroDrive**: 133 green tests including 21 brain-pipeline tests + 6 GEMM + 5 PPO. **Nyquestro**: 88 tests + determinism guard. **Performance Profiler**: xUnit harness caught two production-crash bugs before they shipped. Cross-project investment in correctness-by-tests. |
| Documentation-first iteration / context-as-infrastructure | Proficient | **Cernio**: ~290KB of skill reference documentation as the quality bar; full `context/` folder with architecture/notes/systems/plans. **Aurix**: 1.16MB `context/` with 14 system docs, 11 convention notes, 11 reference papers (~770KB, ~17k lines on V3 math + LP-profitability + backtest-methodology). **Consilium**: 61KB of context docs against 89KB of source in a four-commit repo; reusable `IMPLEMENT_NOW_*` execution-playbook structure. **Open Source Contributions**: 64 markdown notes in umbrella for 0 KB of consumed third-party code. **Chrona**: `plans/NNN_<topic>.md` 11-section schema documenting design ahead of implementation. Recurring across the entire portfolio as a deliberate working style. |
| Live IL injection / production C# | Comfortable | **Performance Profiler**: live IL injection in production C# via MonoMod / Cecil; manipulator rewrites every `ret` instruction in every override of every loaded mod's types, wrapping body in `try/finally` with `ProbeStack.Enter`/`Leave` prologue/epilogue; `_tmlAssembly` filter guarding JIT shared-body trap (reference-type generic instantiations of `ModType<Projectile, ModProjectile>::NewInstance` and `ModType<Player, ModPlayer>::NewInstance` JIT-share a compiled body); `_instrumentedHandles` dedup set keyed on `RuntimeMethodHandle`. Single project, deep production-shipped depth. |
| Compiler / IR design | Familiar | **Xyntra**: typed IR primitives (`NodeID(u32)` newtype, `TensorShape(Vec<usize>)` newtype, `OpKind` enum with transformer-flavoured vocabulary `MatMul, Add, Gelu, Dropout, Softmax, LayerNorm`); HashMap-backed graph with monotonic u32 ID allocation; inputs-only DAG (SSA-style); four-category error taxonomy (`Validation`/`Parsing`/`System`/`Internal`) scaffolded for downstream pipeline stages; validator is `todo!()` stub. **Chrona**: also touches compiler-adjacent surface (CLI parser, value-based error model, repo-discovery walk-up algorithm). Design-ahead-of-use evidence; no completed compiler pipeline shipped. |
| Content-addressed storage / VCS internals | Familiar | **Chrona**: design ambition for content-addressed object store, hashing, trees, commits, refs, index, diff; current implementation is M0 scaffold only (CMake + CLI + error model + repo-discovery walk-up algorithm via `std::filesystem` with documented `exists()` vs `is_directory()` gap). Design-discipline signal, not implementation depth. |
| Network protocols / HTTP server engineering | Familiar | **Zyphos**: thread-per-connection HTTP/1.1 server built bottom-up from raw TCP sockets; `panic::catch_unwind` with payload downcast for liveness; `AtomicUsize::fetch_add(SeqCst)` for connection IDs; hand-written request-line tokenisation; deterministic header ordering reconstructed from `HashMap`; 19 inline parser tests; strict layered module boundaries (`main → handler → router → routes`, `routes → response ← create_responses`). Bottom-up systems-learning project; only 3/30 milestones with code; demonstrates the fundamentals (sockets, parsing, framing) without web framework. |
| Multi-agent LLM orchestration | Comfortable | **Consilium**: structured multi-agent debate with peer-output isolation, 8-key JSON state replacing prose summaries, model-family diversity (`llama3.2` + `qwen3.5:4b` + `gemma3:4b`), sampling-profile layering (strict / balanced / exploratory). **Open Source Contributions / scout-issues skill**: single-message dispatch of N background Opus agents (one per vetted project), each applying cleanliness filters and ranking; cross-project synthesis. **Tessarix**: 7-agent parallel widget production. **Performance Profiler**: 14 background agents in parallel under locked snapshot contracts (~3× speedup, ~25 wall-clock hours for ~76 sequential). Four projects, distinct orchestration patterns. |
| Desktop application engineering (Tauri 2) | Proficient | **Image Browser** + **Aurix** + **Tessarix**: three production-shaped Tauri 2 apps. Tauri 2 over Electron framing repeated with documented rationale (binary size, memory footprint, capability-based security, `assetProtocol` for image loading, ergonomic `ort` integration). Cross-domain (image library + DeFi analytics + interactive learning); pattern explicitly named in LifeOS as "a repeatable Capataina pattern". |
| Profiling-first engineering culture | Comfortable | **Image Browser**: opt-in `--profiling` CLI flag (NOT `--profile` — Tauri 2 owns that); three layers of evidence (spans + 12 named diagnostics + 1 Hz RSS/CPU sampler); on-exit `report.md` with Stall Analysis + Resource Trends; zero overhead when off; explicitly framed "encode validation into runtime; report is the debugger". **Aurix**: telemetry-as-replacement-for-screenshots (Caner verbatim: "every screenshot is so much heavier than a couple more lines in a json file"); 397-line frontend hub + backend persistence; lp.pipeline.* event taxonomy. **Performance Profiler**: profiles itself (Self tab in dashboard: install footprint, bytes/hook, severity bucket, projection to bigger modlists). Three projects with first-class self-observability. |
| Stochastic synthetic flow / simulation | Familiar | **Nyquestro**: OU mean-reverting mid-walk + Poisson per-side arrivals + log-normal sizes; ChaCha8-deterministic. **NeuroDrive**: kinematic physics + analytics sim. **Aurix**: `MockArchiveSource` generates deterministic sinusoidal swap walks. Recurring shape across distinct domains. |
| Honesty-contracted analytics / statistical rigour | Comfortable | **Performance Profiler**: three orthogonal axes (`Confidence × EvidenceScope × BaselineKind`) rendered as separate badges; `PValueAdjusted`-gated promotion (Medium on `pAdjusted <= 0.10`, High on `pAdjusted <= 0.05`); pattern-aware magnitude normalisation (share vs ratio); `InsightsEngine.Shared` lazy singleton ensuring dashboard and session JSON consume same store; rewritten dashboard copy from normative ("possibly removable") to descriptive ("idle most of session"). **Aurix**: explicit recognition that "best-cell LP wins" is selection-bias-inflated; three-variant LP framing as corrective; adaptive-tercile vol-regime classifier; corrected IL semantic (`raw - hold` with fees separate). **AsteroidsAI**: honest documentation of cross-method comparability confounds (parameter count, compute budget, CRN default, fitness penalty, turn deadzone, selection objective, novelty-diversity integration scaling, temporal input). Pattern of refusing to inflate claims when statistical evidence does not support them. |
