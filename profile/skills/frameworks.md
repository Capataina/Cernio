---
title: Frameworks
parent: Skills
last_updated: 2026-05-31
---

# Frameworks

> Application-shaping scaffolds (entry-point structure, threading model, lifecycle, plugin systems). Distinct from Libraries.

| Framework | Proficiency | Evidence |
|-----------|-------------|----------|
| Tauri 2 | Proficient | **Image Browser** (`com.ataca.image-browser`): six pieces of long-lived state via `manage()`; 26 IPC commands grouped by concern; typed `ApiError` `#[serde(tag,content)]` discriminated union; `tauri-plugin-dialog`; assetProtocol for image loading. **Aurix**: 19 IPC handlers; multi-tab shell with `tauri::Builder::default().manage(Arc<Storage>)`; `tauri::ipc::Channel<StreamEvent>` SSE-style streaming consideration. **Tessarix**: Tauri 2 + Vite 7 + React 19; `capabilities/default.json` ACL; `build.rs` invoking `tauri_build::build()`; Windows-specific `[lib].name` workaround for cargo#8519; port-coupling discipline with `strictPort: true` deliberate hard-fail; `Channel<StreamEvent>` token streaming. Three production-shaped Tauri 2 apps. |
| Tokio | Proficient | **Cernio**: async runtime for pipeline scripts; parallel ATS fetches via `fetch_all_parallel` with Tokio semaphore. **Aurix**: `tokio::Builder::new_multi_thread`; `tokio::join!` Tab-1 fan-out across five concurrent futures; 60s tokio-spawned WAL checkpoint task. **Nyquestro**: `tokio` + `tokio-tungstenite` (`native-tls`) for Coinbase WebSocket live-feed; runtime features `rt`, `rt-multi-thread`, `macros`, `net`, `io-util`, `sync`, `time`. **Tessarix**: `tokio` 1 with `rt-multi-thread`, `macros`, `sync` for LLM HTTP client and planned SQLite + Claude API workloads. Cross-domain use across four production projects. |
| Bevy | Comfortable | **NeuroDrive**: seven Bevy plugins + `sim` coordination module; `FixedUpdate` 60 Hz simulation across four causally-ordered `SimSet` stages; `Update` for frame-rate-decoupled rendering; `Last` schedule for shutdown flush; ZST marker components (`PpoCar`, `BrainCar`, `KeyboardCar`) for compile-time partitioning at archetype level (`SideBySide { ppo: 8, brain: 8 }` with zero cross-fleet aliasing); disjoint-query patterns; field-level destructuring for borrow-checker satisfaction. One project at substantial depth. |
| Ratatui | Comfortable | **Cernio**: 26-file Ratatui TUI v5 across 5 views; modular layout (`app/`, `handler/`, `views/`, `widgets/`); custom `widgets/layout.rs::distribute()` for 3 responsive modes; first-class mouse support; semantic colour theme; GitHub-style activity heatmap; `dashboard.rs` is 31.5KB / 946 lines. **Nyquestro**: Ratatui 0.29 + Crossterm 0.28 six-pane dashboard with custom gauges/sparklines/distribution-bars/pressure-bars/health-dot; ANSI-16-only palette + `Color::Reset`; custom `block_bar` 1/8-cell renderer. Two production TUIs across distinct domains. |
| React 19 | Comfortable | **Image Browser**: React 19 + TanStack Query 5 (`staleTime: Infinity`, manual invalidation) + framer-motion + shadcn primitives; theme applied to `<html>` in `main.tsx` before React mounts to avoid FOUC; `useIndexingProgress` subscribes to Tauri events. **Aurix**: React 19.1.0 + Vite 7.0.4; `App.tsx` multi-tab routing; auto-run pipeline keyed off JSON-stringified settings key; `usePersistedState` shape-merge. **Tessarix**: React 19 + lazy-loaded lesson components via `React.lazy`; multi-provider composition (Tier + Settings + MDX); hash routing with `ErrorBoundary`. Cross-project consistency. |
| axum | Familiar | **Cernio**: axum embedded web server on `localhost:7878` over the same SQLite DB (added 2026-05-30); handlers for dashboard, companies, jobs, decisions, activity, detail, ops, api; HTMX inline writes; chip-filter strips with type-based variants; URL-persistent detail drawer. Single substantial integration. |
| Textual | Familiar | **Consilium**: Textual >=8.1.1 TUI with `@work` background-worker pattern for non-blocking provider calls; three-state COMPOSE → RUN → RESULT flow; pipeline-tracker view with stage nodes; subscribes to three service callbacks. One project. |
| HTMX | Familiar | **Cernio web UI**: HTMX 1.9 inline writes from web UI; re-processed inside detail drawer body so Apply/Watch/Reject continue working post-swap; clickable charts cross-filter via `chart.on('click') → location.href`. Single application. |
| LangChain | Familiar | **Consilium**: LangChain (>=1.2.12) core provider-agnostic abstraction; both adapters (`ChatOllama`, `ChatGoogleGenerativeAI`) wrap LangChain clients with uniform `ask(prompt) -> str` surface; `AIMessage.content` defensive duck-typed extraction. Thin-adapter pattern over a single project. |
| TanStack Query 5 | Familiar | **Image Browser**: `staleTime: Infinity` + manual invalidation; mutations explicitly invalidate affected keys; `Phase::Ready` indexing event triggers `invalidateQueries(["images"])`. Single project at production scale. |
| MDX / @mdx-js/rollup | Familiar | **Tessarix**: `@mdx-js/rollup` plugin chain with load-bearing `enforce: "pre"` ordering; `remark-math` → `rehype-katex`; `providerImportSource: "@mdx-js/react"`; two-phase lesson registry (lazy component + eager frontmatter) for tree-shakeable catalog rendering. Single project, deep configuration knowledge. |
| Arcade | Familiar | **AsteroidsAI**: 2D game rendering, windowing, manual-play input; powers both windowed (`AsteroidsGame`) and headless (`HeadlessAsteroidsGame`) variants. Single project. |
| tModLoader 1.4.4 | Familiar | **Performance Profiler**: builds against tModLoader reference assemblies; packs as `.tmod`; `Mod.Load` / `Mod.Unload` lifecycle; `ProfilerSystem : ModSystem` for per-world hooks; tModLoader's `*Loader.HookList<T>` iteration as the instrumentation surface. Single mod-host platform; substantial depth in framework's per-tick override semantics. |
| Cargo workspaces | Comfortable | **Vynapse**: 4-crate workspace (`common → math → core → cli`) with `Cargo.toml`-enforced dependency direction. **Aurix**, **Cernio**, **Nyquestro**: single-member workspaces with deliberate `[lib]`/`[bin]` splits where required for integration testing (Cernio's lib+bin split unblocked 346-test suite). Repeated structural use. |
