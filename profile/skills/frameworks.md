---
title: Frameworks
parent: profile/skills/
last_updated: 2026-05-29
---

## Frameworks

| Framework | Proficiency | Evidence |
|-----------|-------------|----------|
| Tauri 2 | Proficient | Image Browser (`com.ataca.image-browser`; 26 IPC commands organised by concern; `tauri::Builder::manage(...)` for `ImageDatabase`/`CosineIndexState`/`TextEncoderState`/`FusionIndexState`/`IndexingState`/`WatcherHandle`; `tauri::RunEvent::Exit` for on-exit profiling report; `tauri-plugin-dialog` native folder picker; `assetProtocol` solving image loading at gallery scale without base64-over-IPC). Aurix (19 IPC handlers across 3 modules — 3 market, 14 lp, 2 telemetry; `serde(rename_all = "camelCase")` as the only wire-level bridge; `CommandError { message, key_required }` standardised envelope; manual Rust↔TS DTO mirror for 20+ types in 217 lines). Tessarix (3 LLM commands `llm_chat_complete`/`llm_chat_stream`/`llm_chat_json` + telemetry commands; `tauri::ipc::Channel<StreamEvent>` for per-token frontend streaming; capability-permission `capabilities/default.json` with `core:default` + `opener:default`; `Cargo.toml::[lib].name = "tessarix_lib"` for Windows lib/bin collision avoidance per rust-lang/cargo#8519). Cross-domain across image ML / DeFi analytics / learning content. |
| React 19 | Comfortable | Image Browser (functional components, hooks, `React.Profiler` for the masonry grid under `--profiling`, FOUC-free dark mode via pre-React theme application reading `localStorage["theme"]` before mount). Aurix (multi-tab routing via `activeTabId` state with no React Router; StrictMode discipline gating state setters with `mounted` flag while letting both StrictMode mounts run the pipeline; idempotency at storage layer making the second mount a cache hit). Tessarix (`<lazy>` + `<Suspense>` for code-split lessons, five-provider stack `<ErrorBoundary>` → `<SettingsProvider>` → `<TierProvider>` → `<MDXProvider>` → `<Suspense>`, hash-router with discriminated-union route types). |
| Bevy | Comfortable | NeuroDrive (7 plugins + a `sim` coordination module, controller-agnostic boundary between `game/` and `brain/` enforced at module level; `FixedUpdate` schedule at 60Hz with 4-stage `SimSet` ordering Input → Physics → Collision → Measurement; ZST marker components `PpoCar`/`BrainCar`/`KeyboardCar` partitioning learners at compile time via `With<>` query filters; archetype storage making per-archetype query filtering O(1); 8-car vectorised training via `EnvInstanceId` tagging; F4 cycle implemented as despawn-then-respawn to avoid state leak from marker swaps; gizmos for raycast/centreline/crash overlays). Deep single-project use with research-level architectural discipline. |
| Ratatui | Comfortable | Cernio (5 views — Dashboard / Companies / Jobs / Pipeline kanban / Activity — across 26 source files in v5 modular architecture; mouse-first with keyboard accelerators including Ctrl+click multi-select and Shift+click range; responsive layout across Full / Stacked / Compact modes; GitHub-style 7×12 activity heatmap with action-type colouring; ~20 DB query functions in 17.4KB; semantic colour palette with freshness/activity/badge styles). Nyquestro (six-pane dashboard at 33ms render / 50ms sim / 10ms input cadence; ANSI-16-only colour discipline tested against Catppuccin/Solarized-light/Tokyo Night; `block_bar(ratio, width)` sub-cell-precision renderer using `▏▎▍▌▋▊▉█`; mid-price Braille charts; distribution-shape latency bars on log scale with `╫` markers; `PER_FRAME_BUDGET=500` and `SNAPSHOT_LEVEL_CAP=50` as freeze-defence at dispatch and wire boundaries). |
| Tokio | Comfortable | Cernio (`tokio::Semaphore` caps parallel provider calls in `fetch_all_parallel`; business logic stays sync, async is the I/O layer only — matches the "Async Is an Optimization, Not an Architecture" principle). Aurix (multi-thread runtime backing the Rust backend; tokio-rusqlite writer thread serialising mutations; 60-second tokio interval task for `PRAGMA wal_checkpoint(TRUNCATE)`). Nyquestro (live-feed task spawned at startup, main thread reads `FeedEvent`s via `mpsc::Receiver`; engine itself synchronous per D2). Image Browser (background indexing pipeline orchestrated by `tokio::spawn`; single-flight `AtomicBool` with RAII `RunningGuard(Drop)`). Tessarix (`tokio 1` with `rt-multi-thread`/`macros`/`sync` for Ollama HTTP + SSE streaming). Cross-domain across pipelines / DeFi / HFT-style / desktop ML / learning. |
| Vite | Comfortable | Image Browser (Vite 7 with `@vitejs/plugin-react`, `vite-plugin-pages` for FS routing). Aurix (Vite 7.0.4 frontend bundler). Tessarix (Vite 6 with `@mdx-js/rollup` `enforce: "pre"` so `.mdx` files are transformed before `@vitejs/plugin-react` picks them up; `server.port = 1420` with `strictPort: true` matching `tauri.conf.json::build.devUrl`; `clearScreen: false` to keep Rust compile errors visible across HMR; watch-exclusion of `src-tauri/` non-optional to prevent cargo writes thrashing HMR). |
| MDX (`@mdx-js/rollup` + `@mdx-js/react`) | Familiar | Tessarix (`<MDXProvider components={mdxComponents}>` injecting overrides at the App level; remark-math + rehype-katex chain for build-time KaTeX rendering; lesson registry's two-phase loading — eager `Promise<LessonFrontmatter>` for catalog cards plus lazy `Component` for route activation, both paths sharing the same `importer` function reference so Vite tree-shaking works). |
| Textual | Familiar | Consilium (`@work` background-worker pattern for non-blocking provider calls; three-state compose → run → result flow; three completion callbacks driving a pipeline tracker with stage nodes; deliberate UX choice to hide intermediate model output in favour of the final thesis as the visible product, after an earlier multi-panel live-transcript version was discarded). Single dormant project. |
| LangChain | Familiar | Consilium (provider-agnostic LLM abstraction; factory dispatch on `settings.provider`; thin adapters wrapping `langchain_ollama::ChatOllama` and `langchain_google_genai::ChatGoogleGenerativeAI`; defensive duck-typed return handling for `AIMessage.content` shape variance via `getattr(response, "content", "")`; documented provider asymmetry — `repeat_penalty` silently dropped for Google because the SDK does not expose it). LifeOS notes LangChain's 1.x line "still ships breaking changes between minor versions" as a revival-time concern. |
| Arcade (Python game library) | Familiar | AsteroidsAI (`AsteroidsGame(arcade.Window)` for manual play / best-agent playback / fresh-game generalisation capture; sprite-list management; control flags `update_internal_rewards` / `auto_reset_on_collision` / `manual_spawning` / `external_control` exposed for training to configure identically with the headless mode that shares physics constants from `game/globals.py`). Dormant single-project use. |

---
