---
title: Tools and Platforms
parent: Skills
last_updated: 2026-05-31
---

# Tools and Platforms

> Developer tooling, infrastructure, observability, version control, package managers.

| Tool | Proficiency | Evidence |
|------|-------------|----------|
| Git / GitHub | Proficient | Daily use across every project surfaced (Cernio, Image Browser, Aurix, NeuroDrive, Nyquestro, Tessarix, Performance Profiler, Vynapse, AsteroidsAI, Consilium, Chrona, Xyntra, Zyphos, Tectra, Open Source Contributions). Bisectable commit discipline ("six-commit bisectable implementation plan" for fold4d), commit-as-natural-checkpoint discipline (Aurix), per-attempt postmortems (`pr-15453-postmortem.md`), GPG-signing prerequisite awareness for upstream Tauri PRs, force-push policy awareness (resurrection of #15453 as #16119 required fresh PR), per-clone `.git/info/exclude` for scratch state (OSS-contributions umbrella). |
| Cargo | Proficient | Build, test, dependency management for every Rust project; workspace patterns (Vynapse 4-crate); release profile tuning (`lto = "thin"`, `codegen-units = 1` in Nyquestro); lint wiring (`unused_must_use = "deny"`, `needless_collect = "warn"`, `redundant_clone = "warn"`); `assert_cmd` for CLI integration tests; `cargo test --release` for real training runs; `CERNIO_DB_PATH` env-var pattern for tempdir-DB integration tests. |
| GitHub CLI (`gh`) | Comfortable | **Open Source Contributions**: PR creation, issue comments, PR-state polling (`gh api repos/<owner>/<repo>/pulls/<n>`), fork-without-clone (`gh repo fork <owner>/<repo> --clone=false`). **Cernio populate-from-lifeos skill**: cross-vault runtime dependency added session 10; reads `Capataina/LifeOS` and `Capataina/Capataina` README. Repeated use across orchestration workflows. |
| CMake | Comfortable | **Chrona**: CMake 3.20+, project version 0.1.0, language `CXX`; Catch2 v3.5.0 via `FetchContent`; warning level `/W4` on MSVC, `-Wall -Wextra -Wpedantic` on GCC/Clang; CTest enabled with `add_test(NAME chrona_tests COMMAND chrona_tests)`. **Tectra**: 13-line CMakeLists.txt; CMake 3.20+; `-Wall -Wextra -Wpedantic -Werror` from day one; `CMAKE_CXX_STANDARD 20` with `STANDARD_REQUIRED ON`. Two C++ projects, both small but with discipline around warning gates and dependency-fetching. |
| Vite | Comfortable | **Image Browser**: Vite 7 frontend bundler. **Aurix**: Vite 7.0.4; `vite.config.ts::server.port=1420` + `strictPort: true`. **Tessarix**: Vite for WebView + production build; deliberate hard-fail port collision. Repeated. |
| pnpm | Familiar | **Aurix**: pnpm-lock.yaml; pnpm workspace single package. **Tessarix**: pnpm 11+; lockfile 94KB; orchestrated by `tauri-cli`. |
| uv (Python) | Familiar | **Consilium**: dependency management; `pyproject.toml` defines `consilium` script entry point; `unittest discover` via `uv run`. |
| Catch2 / xUnit / vitest / `unittest` | Comfortable | Cross-project testing discipline: Catch2 (Chrona), xUnit (Performance Profiler — `Compile Include` + `Link` keeps tests out of shipped `.tmod`), vitest (Image Browser — 62/62 passing frontend tests), `cargo test` (Cernio 346, Aurix 139, NeuroDrive 133, Nyquestro 88, Vynapse 120, Image Browser 125 cargo lib), `unittest` (Consilium 14 tests in one file). Multiple frameworks under repeated use. |
| `assert_cmd` | Familiar | **Cernio**: CLI subprocess testing against tempdir DBs via `assert_cmd::Command::cargo_bin("cernio")`; 16-test CLI suite enabled by lib+bin split. |
| `loom` / `miri` | Familiar | **Open Source Contributions / tokio target**: documented requirement to use loom-permutation testing for tokio sync/runtime changes (`LOOM_MAX_PREEMPTIONS=1 LOOM_MAX_BRANCHES=10000 RUSTFLAGS="--cfg loom -C debug_assertions"`) and miri for unsafe-path verification. Documented prerequisite knowledge; no merged PR yet. |
| `tracing` + `tracing_subscriber` | Familiar | **Image Browser**: span instrumentation collected by custom `PerfLayer` with `ipc.` / `pipeline.` / `cosine.` / `model_download.` / `watcher.` prefixes; `SpanStats { count, sum, min, max, recent_ringbuffer }`. |
| `sysinfo` | Familiar | **Image Browser**: 1 Hz RSS/CPU sampler thread (sysinfo 0.32). |
| `chromiumoxide` | Familiar | **Cernio**: `cernio snap` CLI driving headless Chrome over web UI tabs and writing per-pane PNGs into `/tmp/cernio-debug/<ts>/`. Also used in broken autofill scaffold. |
| Claude Code skills (native) | Proficient | **Cernio**: 9 native skills under `.claude/skills/`, all skill-creator-audited; YAML engineered triggers with negative-trigger clauses; evidence-anchored mandatory-read tables; What-I-Did-Not-Do declarations between workflow steps; Tier 3 quality checklists; ~290KB of reference documentation as the quality bar. **Open Source Contributions**: `scout-issues` skill dispatches one Opus background agent per vetted project in single message (9 agents in one Claude turn). **Tessarix**: 7-agent parallel widget production. Cross-project, deliberate authoring + orchestration pattern. |
| Multi-agent / worktree-isolated subagents | Comfortable | **Tessarix**: 44-widget linear-algebra push on 2026-05-12 used 7 Opus background agents in worktree isolation (one per lesson). **Performance Profiler**: v0.12's 21-addition tab rework decomposed into 76 atomic tasks across 6 waves, 14 background agents in parallel, downstream agents compiling against locked contract types whose implementations did not yet exist (`Data/Contracts/RolloutContracts.cs`); ~25 wall-clock hours for ~76 hours of sequential work, ~3× speedup. Two projects with disciplined parallel-agent orchestration. |
| Refinery (Rust SQLite migrations) | Familiar | **Aurix**: forward-only refinery migrations embedded in binary. |
| HTMX 1.9 | Familiar | **Cernio web UI**: inline writes; re-processed inside drawer body post-swap. |
| ECharts 5.4 | Familiar | **Cernio web UI**: JSON-island data delivery; clickable chart cross-filter via URL navigation. |
| `notify-debouncer-mini` | Familiar | **Image Browser**: filesystem watcher with 5s debounce. |
| `rayon` | Familiar | **Image Browser**: parallel thumbnail generation (`par_iter`). |
| Python ML stack tooling (Arcade, `ThreadPoolExecutor`) | Familiar | **AsteroidsAI**: `ThreadPoolExecutor(max_workers=os.cpu_count())` for per-generation parallelism (NumPy releases GIL during BLAS); Arcade for windowed + headless modes. |
| Plain CSS (no design system) | Familiar | **Aurix**: 12 per-component CSS files in `src/styles/components/`; deliberate dark-dense trading-terminal aesthetic over Tailwind/shadcn/Chakra defaults. **Tessarix**: 72 `.css` files; dark-luxe palette tokens in `src/theme.css` (159 lines). **Cernio web UI**: modular bundles (`base, motion, components, chrome, filters, debug, ops, drawer, cmdk, presets`). |
| TUI infographics rendering (custom widgets) | Comfortable | **Nyquestro**: gauges, sparklines, distribution-shape bars on log scale, pressure bars, health-dot system, custom `block_bar` 1/8-cell precision renderer, ANSI-16-only palette. **Cernio**: GitHub-style 7×12 activity heatmap, semantic colour theme, kanban with proportional column sizing. |
| LiteDB tooling (`Pragma`) | Familiar | **Performance Profiler**: discovered + worked around BsonValue `Int32` typing trap via tests before shipping; manages `CHECKPOINT=1000` pragma plus periodic checkpoint. |
